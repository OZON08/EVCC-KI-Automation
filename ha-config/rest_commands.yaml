# Diesen Eintrag zum bestehenden rest_command: Block in configuration.yaml hinzufuegen
# (NICHT nochmal rest_command: schreiben, nur den Eintrag darunter)

n8n_restore_ki_sensoren:
  url: "http://homeassistant:8081/webhook/battery-ai-restore"
  method: POST
  headers:
    Content-Type: application/json
  payload: '{"source": "ha_restart"}'
